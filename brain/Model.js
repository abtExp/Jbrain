class Model {
    constructor(type, config) {
        this.type = type;
        this.config = config;
    }

    save() {
        // saves the model;
    }

    load(path) {
        // load a saved model;
    }
}

module.exports = Model;